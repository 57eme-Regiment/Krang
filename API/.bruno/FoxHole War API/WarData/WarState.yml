info:
  name: WarState
  type: http
  seq: 1

http:
  method: GET
  url: "{{process.env.WAR_API_BASE}}/war"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
