info:
  name: Delete Item
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BaseUrl}}/api/items/{{itemId}}"
  auth: inherit

runtime:
  variables:
    - name: itemId
      value: 479cb4d1-6dab-4591-8e3a-e13a42fa50d6

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
