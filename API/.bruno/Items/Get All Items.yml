info:
  name: Get All Items
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseUrl}}/api/items"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
