info:
  name: Create Region
  type: http
  seq: 3

http:
  method: POST
  url: "{{BaseUrl}}/api/regions"
  body:
    type: json
    data: |-
      {
        "name": "Deadlands",
        "gameRegionId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
