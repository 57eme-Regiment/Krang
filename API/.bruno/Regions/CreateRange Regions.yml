info:
  name: "CreateRange Regions "
  type: http
  seq: 4

http:
  method: POST
  url: "{{BaseUrl}}/api/regions/range"
  body:
    type: json
    data: |-
      [
        {
          "name": "Deadlands",
          "gameRegionId": 1
        },
        {
          "name": "Deadlands2",
          "gameRegionId": 2
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
