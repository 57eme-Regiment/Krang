info:
  name: Get All Regions
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseUrl}}/api/regions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
