info:
  name: Upsert Region
  type: http
  seq: 5

http:
  method: POST
  url: "{{BaseUrl}}/api/regions/upsert"
  body:
    type: json
    data: |-
      {
        "name": "Deadlands",
        "gameRegionId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
