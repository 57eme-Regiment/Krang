info:
  name: Create Town
  type: http
  seq: 3

http:
  method: POST
  url: "{{BaseUrl}}/api/towns"
  body:
    type: json
    data: |-
      {
        "name": "Abandoned Ward",
        "longitude": 0.312,
        "latitude": 0.512,
        "regionId": "00000000-0000-0000-0000-000000000000"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
