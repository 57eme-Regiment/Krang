info:
  name: Delete Town
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BaseUrl}}/api/towns/{{townId}}"
  auth: inherit

runtime:
  variables:
    - name: townId
      value: 00000000-0000-0000-0000-000000000000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
