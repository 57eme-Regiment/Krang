info:
  name: Get All Towns
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseUrl}}/api/towns"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
